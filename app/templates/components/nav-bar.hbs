<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class="navbar-brand"}}Project Name{{/link-to}}
    </div>

    <div class="collapse navbar-collapse" id="main-navbar">
      <ul class="nav navbar-nav">
        {{#link-to 'index' tagName="li"}}<a href="">Home</a>{{/link-to}}
        {{#link-to 'about' tagName="li"}}<a href="">About</a>{{/link-to}}
        {{#if session.isAuthenticated}}
          {{#link-to 'posts.new' tagName="li"}}<a href="">New Post</a>{{/link-to}}
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if session.isAuthenticated}}
          {{#link-to 'profile' tagName="li"}}<a href="">{{session.currentUser.displayName}}</a>{{/link-to}}
          <li><a {{action "signOut"}}>Sign Out</a></li>
        {{else}}
          <li>
            <button {{action "signIn" "google"}} class="btn btn-default navbar-btn">Sign in with Google</button>
          </li>
        {{/if}}
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>